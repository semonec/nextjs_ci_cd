import styled from '@emotion/styled'

const Button = styled.button`
  color: hotpink;
  font-size: 14pt;
`

const HotPinkButton = () => {
    return (
        <Button>This is Hotpink button.</Button>
    )
}

export default HotPinkButton